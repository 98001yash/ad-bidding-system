apiVersion: v1
kind: Service

metadata:
  name: campaign-service
  namespace: ad-bidding-system

spec:
  selector:
    app: campaign-service

  ports:
    - port: 8090
      targetPort: 8090
      nodePort: 30090

  type: NodePort