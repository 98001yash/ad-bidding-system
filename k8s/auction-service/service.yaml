apiVersion: v1
kind: Service

metadata:
  name: auction-service
  namespace: ad-bidding-system

spec:
  selector:
    app: auction-service

  ports:
    - port: 8092
      targetPort: 8092

  type: ClusterIP