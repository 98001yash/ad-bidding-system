apiVersion: v1
kind: Service

metadata:
  name: prometheus
  namespace: ad-bidding-system

spec:
  selector:
    app: prometheus

  ports:
    - port: 9090
      targetPort: 9090
      nodePort: 30091

  type: NodePort